
// each box is a collection of gears
export default function box(name, gears) {

}

// name of input port + name of box 
export default function in(name, box) {

}

// name of output port + name of box 
export default function out(name, box) {

}

// name of connection && name of output port + name of input port
export default function connect(name, out, in) {

}